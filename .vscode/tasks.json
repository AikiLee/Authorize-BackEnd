{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "tsc-build",
            "command": "${workspaceFolder}/node_modules/.bin/tsc",
            "args": ["-p", "tsconfig.json"],
            "type": "shell",
            "problemMatcher": ["$tsc"]
        }
    ]
}